// parse函数，就是主函数
export default function parse(templateStr) {
  // 指针
  var index = 0
  while (index < templateStr.length - 1) {
    console.log(templateStr[i])
    index++
  }
  return templateStr
}
